<h1>Listing posts</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Game Title</th>
    <th>Game Username</th>
    <th>Text</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.title_of_the_game %></td>
      <td><%= post.game_username %></td>
      <td><%= post.text %></td>
      <td><%= link_to 'Show', post_path(post) %></td>
    </tr>
  <% end %>
</table>